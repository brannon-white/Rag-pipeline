"""Evaluation harness: golden dataset construction and retrieval metrics.

Kept out of the runtime image (see ``pyproject.toml``'s ``evals`` extra) --
the API container must not carry the eval/judge dependency surface.
"""
